'use client';

// Alias for backwards compatibility with existing templates and builder items
import CloudinaryUploadWidget, { CloudinaryUploadWidgetProps } from './CloudinaryUploadWidget';

export type SupabaseUploadWidgetProps = CloudinaryUploadWidgetProps;
export default CloudinaryUploadWidget;
